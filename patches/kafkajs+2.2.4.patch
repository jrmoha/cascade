diff --git a/node_modules/kafkajs/src/network/requestQueue/index.js b/node_modules/kafkajs/src/network/requestQueue/index.js
index eee14d6..b9ee7a4 100644
--- a/node_modules/kafkajs/src/network/requestQueue/index.js
+++ b/node_modules/kafkajs/src/network/requestQueue/index.js
@@ -313,6 +313,12 @@ module.exports = class RequestQueue extends EventEmitter {
     if (!this.throttleCheckTimeoutId) {
       if (this.pending.length > 0) {
         scheduleAt = scheduleAt > 0 ? scheduleAt : CHECK_PENDING_REQUESTS_INTERVAL
+      } else if (scheduleAt <= 0) {
+        // Nothing pending and no active client-side throttle: there is nothing
+        // to re-check, so avoid scheduling a setTimeout with a negative delay
+        // (which triggers Node's TimeoutNegativeWarning and a pointless ~1ms
+        // re-arm loop).
+        return
       }
       this.throttleCheckTimeoutId = setTimeout(() => {
         this.throttleCheckTimeoutId = null
